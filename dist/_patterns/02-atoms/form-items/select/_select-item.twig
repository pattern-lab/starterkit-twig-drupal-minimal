{% set option_value = option_value|default(select_item) %}
{% set option_selected = option_label|default('false') %}
<option value="{{ option_value }}"{{ option.selected ? ' selected="selected"' }}>{{ select_item }}</option>
